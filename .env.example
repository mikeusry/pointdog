# point.dog environment variables
# Copy to .env for local dev. Set in Vercel for production.

# SendGrid — required for /api/contact notification emails
# Notifications go to mike@point.dog. From: noreply@point.dog (point.dog
# domain authenticated in SendGrid).
SENDGRID_API_KEY=
